model Category {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?
  slug        String  @unique

  jobs Job[]

  created_at DateTime  @default(now())
  updated_at DateTime? @updatedAt
  deleted_at DateTime?

  @@map("categories")
}
